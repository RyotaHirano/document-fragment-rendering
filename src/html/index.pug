<!DOCTYPE html>
html(lang="ja")
  head
    meta(charset="UTF-8")
    meta(http-equiv="X-UA-Compatible" content="IE=edge")
    title document fragment rendering
    meta(name="viewport", content="width=device-width,initial-scale=1")
  body
    h1 document fragment rendering

    .times
      .time
        p create table-a time
        span.js-table-a-time
        span (ms)

      .time
        p create table-b time
        span.js-table-b-time
        span (ms)

    .tables
      table.js-table-a
        thead
          tr
            th id
            th name
            th image
            th price
        tbody

      table.js-table-b
        thead
          tr
            th id
            th name
            th image
            th price
        tbody
